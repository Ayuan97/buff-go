# todo

> 编号表示实施阶段，`Goal 1A` 这类子项才是一次实际 Goal。一次只执行一个 Goal；完成后勾选并在 `work.md` 记录验收结果。

## 执行原则

- 不把目录移动与业务行为改写放进同一个 Goal。
- 不同时迁移多个平台，也不同时开发全部 API 和全部页面。
- 目标目录只有出现真实实现时才创建，不添加空包、占位页面、伪接口或演示数据。
- 除 `cmd/buffgo` 和 `internal/app` 的临时装配桥接外，新业务包不得反向依赖旧 `internal/buffgo`；旧调用方可以依赖已经迁移完成的新包。
- 每个 Goal 开始前记录测试基线，结束后执行与改动范围相符的验收。
- Goal 0B 不阻塞目录基线、装配和领域整理，但必须在 Goal 6A 前完成；BUFF、IGXE 的证据 Goal 同理阻塞各自实现。
- 每个 Goal 的输入、变更边界、产物位置、实际验收命令和结果统一写入 `work.md`，不能只写“已完成”。

## 阶段 0：产品文档与平台证据

### Goal 0A：目标文档

- [x] 定义产品目标、Steam 商品基准、bid/ask 和两阶段采集边界。
- [x] 定义 Go 后端、TypeScript Web 控制台和 PostgreSQL 的目标系统形态。
- [x] 分开维护 README、ARCHITECTURE、REFERENCE、CONSOLE 和平台接口资料。
- [x] 核验现有 `testdata` 的证据边界，不把文件名和样例推断当成正式接口契约。

验收：独立读者能复述产品主线、系统边界、控制台首版范围和实施顺序。

### Goal 0B：Steam 接口证据

- [ ] 重新探测 Steam 商品列表、求购、出售和详情接口，保存请求条件、成功、空数据、登录失效和限频证据。
- [ ] 使用登录后的人民币账号确认 Steam 的币种、价格单位、手续费和账号地区关系。
- [ ] 确认 Steam 标准商品的正式标识组合，以及 `market_hash_name`、`classid` 等字段的真实作用。

> 2026-08-11 首轮受控复探在第一个匿名 `market/search/render` 请求即收到无 `Retry-After` 的 `429`，随后零重试停止，真实 Cookie 未发送。三项验收均保持未完成。使用者确认 Steam `429` 通常较快恢复，但这只是项目输入，不能作为固定冷却时长；具体恢复时间和限制范围仍需受控复探确认。

验收：进入实现的 Steam 字段都达到“已验证”，未知项不会进入正式采集。

## 阶段 1：目录迁移基线

### Goal 1A：迁移清单

> 建议下一次启动 Goal 时只完成本项，不移动源码。

- [x] 记录当前 `go test ./...`、`go vet ./...` 和构建结果。
- [x] 输出当前包依赖关系，并逐包标记为“原样迁移、拆分迁移、暂留或删除候选”。
- [x] 为每个现有包确定唯一目标职责，重点拆清 `pipeline`、`source`、`pool` 和 `catalog` 的混合责任。
- [x] 确定迁移批次及每批入口、调用方、测试和回退边界。
- [x] 核对当前 Git 变更归属，确保迁移不恢复旧项目、不覆盖现有重构成果。

验收：每个现有包都有目标去向和迁移方式；后续每批都能单独测试；源码路径与业务行为保持不变。

### Goal 1B：Telemetry 纯迁移

- [x] 将 `internal/buffgo/observ` 原样迁移到 `internal/telemetry`。
- [x] 只更新包名、导入路径和调用标识，不调整事件、日志或统计语义。
- [x] 清除所有 Go 源码中的旧 `internal/buffgo/observ` 引用。

验收：telemetry、pool、pipeline、run 及全量测试通过；`go vet ./...`、`go build ./...` 通过；旧路径零引用。回退边界只包含目录、包名和导入。

### Goal 1C：Telemetry 敏感字段保护

> 本 Goal 必须在使用真实平台账号或代理运行采集测试前完成。

- [x] 明确 telemetry 允许记录的平台、任务和资源安全标识，不记录账号会话、代理认证或完整请求信息。
- [x] 对 `Account`、`Proxy` 和自由文本 `Detail` 建立统一脱敏边界，并覆盖日志与内存记录器测试。
- [x] 验证限频、占用和失败诊断仍可关联同一安全资源标识。

验收：测试注入敏感值后，日志、内存事件和错误摘要均不出现原文；同一进程内可按平台、来源、任务、账号、节点、Worker 和 Lease 的安全引用完成关联。跨重启且可读的账号/节点别名由 Goal 5A 的持久资源模型提供。

## 阶段 2：进程入口与装配

### Goal 2A：可执行入口

- [x] 建立真实的 `cmd/buffgo` 入口和 `internal/app` 生命周期，只负责依赖装配、启动和优雅停止。
- [x] 允许装配桥接层临时调用现有能力，不在本 Goal 改写采集、存储或平台逻辑。
- [x] 验证启动失败、系统信号取消和资源关闭顺序。

验收：可执行程序可以启动并可靠退出；业务输出保持不变；临时旧包依赖只出现在装配桥接层。

## 阶段 3：稳定领域语义与迁移

### Goal 3A：市场语义改造

- [x] 在现有路径先固定 bid/ask、人民币分整数、数量、时间、行情状态和完整性契约。
- [x] 删除统一行情中的多币种换算入口，并为预期行为变化建立测试。

验收：新测试证明系统只接受明确人民币、金额使用分整数、求购与出售及空/失败/部分状态不混淆；本 Goal 不移动目录。

### Goal 3B：纯迁移市场包

- [x] 将已经稳定的市场契约原样迁移到 `internal/market`，只更新调用方和导入路径。

验收：迁移前后的市场测试与外部行为一致；`internal/market` 不依赖 HTTP、PostgreSQL、具体平台或旧包。

### Goal 3C：商品身份语义改造

- [x] 在现有路径先固定 Steam 标准商品、平台商品、精确标识映射和同 `appid` 唯一名称兜底。
- [x] 为映射成功、失败和歧义建立测试。

验收：映射行为符合 REFERENCE；本 Goal 不移动目录，也不访问平台或数据库。

### Goal 3D：纯迁移商品包

- [x] 将稳定商品身份行为迁移到 `internal/catalog`，拆出原 `catalog`、`nameid` 和 `resolve` 中只属于该领域的部分。

验收：迁移前后的映射测试与外部行为一致；`internal/catalog` 不依赖 HTTP、PostgreSQL、具体平台或旧包。

## 阶段 4：行情基础持久化

### Goal 4A：商品、映射与行情表

- [x] 建立 `internal/storage/postgres` 和包内 `migrations/` 执行边界。
- [x] 只持久化 Steam 商品、平台商品映射、最新采集尝试和最近有效价格。
- [x] 使用约束保证人民币分整数、唯一映射、行情状态和 `(product_id, platform, side)` 身份合法。

验收：最新尝试与最近有效价格可独立读取；空、失败和不可用不会把历史价格伪装成当前值；本 Goal 不提前创建资源、运行、冷却、规则或详情表。

## 阶段 5：资源与限频

### Goal 5A：账号和访问节点

- [x] 建立平台账号、访问节点、地域能力和平台独占分配模型及 PostgreSQL 表。
- [x] 将本机直连、国内、国外和香港代理统一为访问节点。
- [ ] 只接收静态或粘性出口节点，验证并保存实际出口 IP、验证时间、`valid_until` 和可用状态。
- [x] 加密保存账号会话和代理认证信息，密钥与数据库分离；日志、错误和查询只暴露脱敏状态。

验收：节点只能分给一个平台；地域约束明确；验证过期或代理配置、凭据、粘性会话变化后必须重新验证，无法稳定识别出口的节点不可用；重启后资源配置可恢复；数据库副本不能单独还原账号会话或代理认证明文。

### Goal 5B：显式账号节点组合

- [x] 建立账号节点组合和运行时独占占用。
- [x] 实现解除、重新分配和删除前的占用保护。

验收：同一账号和节点不会被并发组件重复使用；显式释放和组件级取消后占用可靠释放。真实采集关闭在 Goal 7C 验收。

### Goal 5C：限频与冷却

- [x] 建立平台、接口、账号、IP、账号与 IP 组合的限频与冷却模型及 PostgreSQL 表。
- [x] 保证同平台 bid/ask 共享总预算，不同平台冷却互不串联。
- [x] 使用实际出口 IP 作为 IP 键，相同出口节点共享预算，出口变化后切换到新 IP 状态。

验收：限频组件只冻结证据支持的范围，冷却可跨重启恢复。真实平台请求是否全部经过准入，在各平台接入和端到端 Goal 分别验收。

## 阶段 6：Steam 单次摘要采集

### Goal 6A：Steam 商品目录

- [ ] 按已验证接口同步 Steam 标准商品目录。

验收：使用 Steam 账号节点组合完成一次受限同步；商品身份和字段来源可追溯；分页中断不污染已存在目录。

### Goal 6B：Steam 出售摘要

- [ ] 实现 Steam `best_ask` 的人民币列表采集、逐页规整和幂等保存。

验收：通过可重复的单次调用采集人民币出售摘要；语义、分页和空数据均由接口证据支持；真实请求全部经过资源占用和限频准入。

### Goal 6C：Steam 求购摘要

- [ ] 按独立验证的求购接口实现 Steam `best_bid`，不复用只有 sell 字段的样例推断。

验收：通过可重复的单次调用采集人民币求购摘要；语义、分页和空数据均由接口证据支持；真实请求全部经过资源占用和限频准入。

## 阶段 7：常驻运行与控制 API

### Goal 7A：运行状态与持久化

- [x] 建立 `catalog`、`summary`、`detail` 任务类型，以及期望状态、实际状态、运行批次、游标、完整性和相应 PostgreSQL 表。
- [x] 持久化 `(steam, appid)` 目录目标的开关和正数周期。
- [x] 固定 waiting、blocked、error 以及临时失败后的下一周期恢复语义。
- [x] 使用开关版本、运行序号和页面序号防止旧运行与迟到响应覆盖新状态。

验收：目录任务不要求方向，摘要和详情任务要求方向；状态转换和跨重启读取测试通过；旧版本、迟到响应和部分运行不会批量污染当前行情；本 Goal 不实现调度循环或 HTTP 接口。

### Goal 7B：单周期调度

- [x] 实现一次调度周期、页面请求级资源占用、逐页提交和部分成功。
- [x] 在不同游戏、方向和任务间使用多个组合并行，同一顺序游标流保持串行。
- [x] 让 Steam 目录同步按独立开关与周期参与调度，不受 bid/ask 开关影响。
- [x] 调度前拒绝出口验证过期或正在重新验证的节点，确认新出口后只使用新 IP 限频键。

验收：测试覆盖等待资源、冷却、登录失效、超时、资源独占、逐页释放、并发容量上限和出口变化；出口变化后没有请求继续使用旧 IP 限频键；本 Goal 不实现常驻循环或关闭恢复。

### Goal 7C：常驻循环、停止与恢复

- [x] 实现单进程常驻调度、关闭取消、另一方向继续和异常重启恢复。

验收：关闭单一方向后不再派发新请求，另一方向和独立目录同步继续运行；关闭一个目录目标不影响其他 `appid` 或摘要方向；已提交目录页保留且执行占用可靠释放；开关仍启用的临时失败在下一周期新建运行；重启后未超龄的运行从存量游标续采而不重采已提交页；同一份采集状态同时只有一个常驻进程，锁失效即停止写入。

### Goal 7D：本地 API 安全基础

- [x] 创建 `api/openapi.yaml`，定义安全上下文 GET、控制会话 Cookie、CSRF 专用请求头和通用错误。
- [x] 建立 API 路由，校验允许的 Host、同源 Origin、JSON POST 与 CSRF 令牌。
- [x] 禁止跨域，保证 GET 无副作用；为 API 响应设置严格 CSP。
- [x] 由同源安全上下文 GET 创建短期控制会话并签发绑定令牌；使用 `HttpOnly`、`SameSite=Strict` Cookie，POST 通过专用请求头提交令牌。

验收：合法同源 GET/POST 可用；缺失、错误、过期或会话不匹配的令牌，以及 DNS rebinding、跨源、表单 POST 和不允许的 Host 均被拒绝；安全上下文不修改业务数据；敏感字段不进入响应或日志。

> 2026-08-12：7D 只完成安全基础和可选 HTTP 接线，业务资源 API 留给 7E～7H。`cmd/buffgo -api-listen 127.0.0.1:<port>` 才启动控制面；默认启动行为保持 legacy runtime。监听地址只接受显式回环 IP，不接受域名、通配地址或公网地址。

### Goal 7E：账号管理 API

- [ ] 扩展已有 OpenAPI，实现账号新增、替换、删除和状态查询的 GET/POST 契约。

验收：OpenAPI 与 Go 行为一致；活动账号受占用保护；查询不返回原始会话内容。

### Goal 7F：节点、分配与组合 API

- [ ] 实现访问节点、只写代理认证信息、平台独占分配和账号节点组合的 GET/POST 契约。

验收：地域、独占、组合和占用冲突可被 API 准确表达；查询不返回代理认证信息；没有自动借用或隐式组合。

### Goal 7G：采集控制与状态 API

- [ ] 实现 Steam 目录同步与平台方向摘要的启停、期望/实际状态、运行、完整性和冷却诊断 GET/POST 契约。
- [ ] 为 `(steam, appid)` 目录目标实现周期读取与修改、立即同步 POST 和活动运行幂等返回。

- [ ] 装配常驻调度：DSN 显式设置 `tcp_user_timeout` 与 TCP keepalive，否则主机宕机后实例锁要等约 2 小时才释放，接替进程一直被 `ErrInstanceLocked` 拒绝。
- [ ] 让创建运行冲突（`executeRun` 的 detached 分支）产生可观测错误，并提供 owner 授权的强制结束运行入口：状态修复入口开放后，旧开关的残留活动运行会让目标静默停在 running 且永不采集。

验收：目录同步不带方向且独立控制；禁用目标拒绝立即同步，活动目标重复触发返回同一运行，周期从下一轮生效；关闭请求只在目标任务退出后成为 stopped；等待、阻塞和错误不会混淆；常驻进程被强杀后接替进程能在秒级内取得实例锁。

### Goal 7H：概览 API

- [ ] 实现目录同步、平台方向状态、最近运行、过期行情、冷却资源和映射失败的只读聚合接口。

验收：OpenAPI 与真实数据库状态一致；没有占位字段或未实现接口；Web 工程存在前不生成客户端。

## 阶段 8：Web 基础与概览

### Goal 8A：真实 Web 概览

- [ ] 创建单一 `web/` 工程，使用 Vue 3、TypeScript、Vite 和 npm，不引入额外 Node 后端。
- [ ] 从 OpenAPI 生成 TypeScript 客户端并统一注入 CSRF 令牌，令牌过期时重新获取安全上下文且不静默重放业务 POST，完成概览的加载、空、错误和异常跳转状态。

验收：概览只显示真实 API 数据；OpenAPI、Go 实现、生成客户端和页面完成第一个纵向闭环。

### Goal 8B：静态文件嵌入

- [ ] 配置前端构建产物生成到被 Git 忽略的 `internal/webui/dist/`。
- [ ] 由 Go 进程嵌入并提供静态文件，生产不运行 Node 服务。
- [ ] 固定验证顺序为 `npm ci`、类型检查、前端构建、Go 测试与编译。

验收：干净工作区按固定命令可构建；Go 二进制可打开真实概览；桌面与窄屏关键状态无遮挡。

## 阶段 9：Web 控制纵向功能

### Goal 9A：采集开关

- [ ] 贯通 Steam 目录同步和平台方向摘要的开关持久化、POST 操作、GET 状态和页面。

验收：目录同步不受 bid/ask 开关影响；页面支持周期、立即同步和活动运行跳转；禁用时不能立即同步，重复点击不产生并行运行；页面区分期望状态、启动中、等待中、运行中、已阻塞、停止中、已停止和错误，不把受理显示成完成。

### Goal 9B：平台账号

- [ ] 贯通账号添加、替换、删除、会话状态和占用保护。

验收：活动账号不能被直接替换或删除；查询不返回原始会话内容；替换后旧会话不再参与新请求。

### Goal 9C：访问节点

- [ ] 贯通节点添加、删除、代理凭据替换、地域、本机直连、健康和占用保护。

验收：地域与平台约束有效；页面显示实际出口 IP、验证时间、有效期和重新验证状态；共享出口不会虚增并发；出口过期或不稳定节点不可用；被占用节点不能删除或替换凭据；保存后不回显代理认证信息。

### Goal 9D：分配与组合

- [ ] 贯通节点平台分配、解除、账号节点组合和冲突校验。

验收：不存在自动跨平台借用或隐式笛卡尔组合；冲突操作返回可解释原因。

### Goal 9E：运行与冷却

- [ ] 贯通运行批次、游标、完整性、错误、资源占用和只读冷却诊断。

验收：使用者能判断任务完成到哪里、使用什么资源、为何冷却或失败。

## 阶段 10：Steam 市场数据页面

### Goal 10A：Steam 行情纵向功能

- [ ] 定义并实现 Steam 商品与行情查询的 OpenAPI、Go 接口和生成客户端。
- [ ] 以 Steam 商品为中心展示人民币 `best_bid`、`best_ask`、最新尝试、最近有效价格、时间、完整性和过期信息。

验收：页面只使用真实 Steam 数据，不混淆求购与出售、最新尝试与历史有效价格、空数据与失败、最新与过期、完整与部分覆盖。

## 阶段 11：第三方平台接入

### Goal 11A：BUFF 接口证据

- [ ] 验证 BUFF 的登录、Steam 标识、人民币、bid/ask、分页、数量、空数据和限频语义。

验收：正式采集需要的字段全部达到“已验证”。

### Goal 11B：BUFF 摘要采集

- [ ] 实现 BUFF 求购和出售摘要、精确映射、资源调度和逐页保存。

验收：非人民币、映射不唯一或字段语义不明的数据不进入统一行情；BUFF 真实请求全部经过资源占用和限频准入。

### Goal 11C：BUFF 跨平台行情与映射

- [ ] 扩展行情与映射查询 OpenAPI、Go 接口、生成客户端和页面，展示 Steam 与 BUFF 数据及映射失败。

验收：页面只使用真实 Steam 与 BUFF 数据；每项映射可追溯到平台标识或同 `appid` 唯一精确名称兜底，不使用演示数据。

### Goal 11D：IGXE 接口证据

- [ ] 在 BUFF 验收后，以相同标准独立调查 IGXE。

验收：正式采集需要的字段全部达到“已验证”。

### Goal 11E：IGXE 摘要采集

- [ ] 实现 IGXE 求购和出售摘要、精确映射、资源调度和逐页保存。

验收：IGXE 错误和冷却不错误影响其他平台；IGXE 真实请求全部经过资源占用和限频准入。

### Goal 11F：IGXE 跨平台页面扩展

- [ ] 扩展查询契约、生成客户端和页面以展示 IGXE 行情与映射失败。

验收：页面使用真实 IGXE 数据，且不会改变 Steam 与 BUFF 的既有语义。

## 阶段 12：规则与详情

### Goal 12A：规则与触发

- [ ] 建立第一批摘要规则、版本、触发依据、详情能力门槛、幂等任务及 PostgreSQL 表。

验收：相同规则版本和摘要版本只产生一个有效详情任务；详情能力未验证或未实现时只记录 `detail_unavailable`，不创建执行任务。

### Goal 12B：订单详情采集

- [ ] 先只实现已有详情接口证据覆盖的 Steam 商品与方向订单详情分页采集及详情表。

验收：Steam 详情保留属性、采集窗口和完整性；部分结果不覆盖完整快照；不推测第三方详情字段。

### Goal 12C：规则与详情页面

- [ ] 定义并实现规则启停、触发记录和详情查询的 OpenAPI、Go 接口、生成客户端和页面。

验收：页面可解释为何触发，且不会把部分快照展示为完整订单列表。

### Goal 12D：BUFF 详情证据

- [ ] 验证 BUFF 详情接口的订单、属性、分页、人民币、登录和限频语义。

验收：BUFF 详情实现需要的字段全部达到“已验证”，本 Goal 不修改适配器。

### Goal 12E：BUFF 详情实现

- [ ] 只使用 Goal 12D 已验证字段实现 BUFF 详情采集。

验收：BUFF 部分详情不覆盖完整快照，也不错误影响其他平台冷却。

### Goal 12F：IGXE 详情证据

- [ ] 验证 IGXE 详情接口的订单、属性、分页、人民币、登录和限频语义。

验收：IGXE 详情实现需要的字段全部达到“已验证”，本 Goal 不修改适配器。

### Goal 12G：IGXE 详情实现

- [ ] 只使用 Goal 12F 已验证字段实现 IGXE 详情采集。

验收：IGXE 部分详情不覆盖完整快照，也不错误影响其他平台冷却。

## 阶段 13：系统验收与远程访问

### Goal 13A：端到端验收

- [ ] 验证从 Web 配置资源到采集、规整、展示和按需详情的完整闭环。
- [ ] 验证启停、限频、部分分页、重启恢复和危险操作保护。
- [ ] 验证所有真实平台请求都经过资源占用与限频准入，且分配节点数不会被误报为有效并发数。

验收：README 中的产品验收标准全部有可重复证据。

### Goal 13B：可选远程访问

- [ ] 只有确认需要从其他设备访问时，才补齐认证、TLS 和安全配置。

验收：默认仍只监听本机；远程入口不存在无认证明文暴露。
